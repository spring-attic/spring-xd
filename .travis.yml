language: java

services:
  - rabbitmq
  - redis-server

sudo: false

cache:
  directories:
  - $HOME/.gradle

env:
  - TERM=dumb

script:
  - ./gradlew --debug clean build -PtestLoggingStarted=true -PwikiBranch=origin/master

#test