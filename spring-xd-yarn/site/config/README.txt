The configurations for all modules can be customized by modifying the file 
modules.yml in this directory and then adding it to the modules-config.zip 
archive. You can run the following command from this directory to achieve this:

  jar -uf modules-config.zip modules.yml

